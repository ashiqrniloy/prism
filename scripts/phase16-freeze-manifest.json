{
 "$comment": "Phase 16 (0.1.4) Task 0 god-module-split scope gate. Machine-checkable source of truth consumed by Tasks 1-4 of plans/016-Release-0-1-4-God-Module-Split.md: the allowed compat-preserving split changes (move declarations/free functions out of src/agents.ts and src/contracts.ts into sibling modules behind barrel re-exports in those two files; reuse of the already-split agent-run-state.ts fingerprint module and agent-run-lifecycle.ts run-lifecycle module; a zero-dep tree-shake bench; package.json test-script wiring; docs updates; version bump 0.1.3->0.1.4; compat baseline regeneration), the forbidden items (public-export removal/rename/signature change, behavior/algorithm/test-expectation change, RuntimeAgentSession class split, 0.1.5/0.1.6/0.1.7/0.2.0 work, new packages/subpaths, new runtime or dev dependencies incl. any bundler, ponytail-comment deletion, new public exports), the identical-surface compat promise (the union .d.ts surface stays byte-identical; only the version literal changes), the audit/signed-tag/provenance policy, per-task evidence tokens, and the split-freeze deviation log. This is a scope gate, NOT a release-contract support matrix; the 0.1.x support matrix stays frozen at scripts/phase12-freeze-manifest.json and 0.1.4 changes none of it. Amendment requires a recorded deviation entry below. Validated by scripts/phase16-freeze.test.mjs. Baseline evidence for regression comparison at the Task 4 exit gate lives in scripts/phase16-baseline.json.",
 "release": "0.1.4",
 "line": "0.1.x",
 "type": "god-module-split",
 "baseline": "0.1.3 (plan 015 exit gate passed 2026-08-10; signed tag v0.1.3; 49 publishable manifests)",
 "splitFreeze": {
  "active": true,
  "allowedChanges": [
   "contracts.ts split: move declarations verbatim from src/contracts.ts into src/contracts-run-state.ts (run-state + approval/decision contracts) and src/contracts-protocol.ts (ProviderEvent/AgentEvent/RealtimeEvent/ToolEffect*/RunRecord/RunLedger/protocol-payload contracts), and optionally src/contracts-core.ts (the remaining JSON/content/message/model/provider/tool/command/context/skill/extension/manifest/store contracts); src/contracts.ts becomes a barrel (export * from each split module) keeping the existing tail alias re-exports for identity.js/persistence-lifecycle.js; cross-file import type edges fixed; src/index.ts line 128 (export type * from ./contracts.js) unchanged",
   "agents.ts split: move free functions and the RuntimeAgentSession/EventSubscriber classes whole from src/agents.ts into src/agent-approval.ts (pendingDecisionsOf/resolveRunDecisions/validateModifiedArguments/decisionScopesEqual/nestedApprovalId/pathsEqual/nestedOutcomeToolResult/decisionIdentityRef/AgentRunSuspended), src/agent-tool-dispatch.ts (toolElicitationRequest/validateElicitationPayload/safeToolElicitationValidate/activeTools/policyList), src/agent-resume.ts or an extension of the existing agent-run-lifecycle.ts (prepareAgentRunResume/executePreparedAgentRunResume/resumeAgentRun/resumeAgentRunStream), and src/agent-session.ts (RuntimeAgentSession + EventSubscriber + createAgent + createAgentSession + remaining helpers); src/agents.ts becomes a barrel re-exporting the four public exports and the moved internals; reuse the existing agent-run-state.ts (agentFingerprint/agentId) and agent-run-lifecycle.ts (createAgentRunLifecycle) which are already split and already re-exported through src/index.ts",
   "tree-shake bench: scripts/phase16-tree-shake.mjs (zero-dependency) + scripts/phase16-tree-shake.test.mjs, recording dist/agents.js + dist/contracts.js + dist/agents.d.ts + dist/contracts.d.ts byte sizes, dist/*.js and dist/*.d.ts module counts, and a static-import reachability count from dist/agents.js, compared before (scripts/phase16-baseline.json splitBaseline) vs after; reachability is reported non-blocking (a static-graph proxy, not a real-bundle measure)",
   "package.json scripts/files edits and release tooling and evidence scripts (phase16 freeze/baseline/tree-shake, docs tripwires); wiring scripts/phase16-freeze.test.mjs (and later scripts/phase16-tree-shake.test.mjs) into the npm test node --test list",
   "docs updates: CHANGELOG.md 0.1.4 entry (internal split, zero public-surface change, measured tree-shaking improvement), docs/index.md current line -> 0.1.4, docs/migration.md 0.1.3 -> 0.1.4 section (internal file reorganization behind barrel re-exports; public declaration surface unchanged; no migration step), docs/agent-session-runtime.md and docs/public-contracts.md internal-structure notes (no public API page change)",
   "version bump 0.1.3 -> 0.1.4 across 49 publishable manifests + lockfile + version-sensitive pins (src/index.ts version const, index.test.ts, release.test.ts graph test, docs.test.ts root-manifest assertion + plan 016 tripwire, install-smoke.test.ts, packaging.test.ts, workspace index.test.ts pins); compat baseline regenerated via release:gate --version 0.1.4 --update-baseline with zero added/removed/changed deltas vs the 0.1.3 surface (only the version literal changes)",
   "browser CDP feature (Tasks 4-5, scope amendment 2026-08-10): packages/browser gains a CDP session layer over playwright-core's existing optional-peer CDP transport (browser.newBrowserCDPSession / browserContext.newCDPSession; zero new deps; host-supplied browser only, never launches browsers), new tools browser_evaluate (bounded Runtime.evaluate, execution-policy gated) + browser_observe (bounded console/network observation ring, no body capture), raw css/xpath act targets, and CDP-backed block_urls/unblock_urls/throttle/emulate act actions; additive exports from @arnilo/prism-browser only (new tools/types/limits/error codes + optional newBrowserCDPSession?/newCDPSession? members on PlaywrightBrowser/PlaywrightBrowserContext); BrowserCdpOptions mode auto/on/off; run-scoped state reset on browser_close",
   "browser CDP tests/docs/baseline (Task 5): packages/browser/src/__tests__/{cdp,evaluate,observe}.test.ts fakes-based unit tests + live Chromium leg in test:live; docs/browser-automation.md CDP section + docs/index.md browser line + packages/browser/README.md; @arnilo/prism-browser baseline regeneration at the exit gate with only the documented added + optional-member deltas (deviation #2); freeze-manifest task tokens task4/task5 move to done with evidence"
  ],
  "forbiddenChanges": [
   "public-export removal, rename, or declaration signature change (the union .d.ts surface in scripts/compat-baseline/arnilo__prism.txt must stay byte-identical; only the version literal changes; release:gate --version 0.1.4 reports 0 added / 0 removed / 0 changed)",
   "any runtime behavior, algorithm, or test-expectation change (the split is a pure move; the existing npm test suite must pass unchanged in count and outcome)",
   "splitting the RuntimeAgentSession class across files (TypeScript classes do not split cleanly; the class moves whole into agent-session.ts)",
   "new public exports or new re-exports from src/index.ts (the 0.1.4 surface is frozen identical to 0.1.3; internal-only moves)",
   "0.1.5 items: deprecated-option removal (ProviderRequestOptions.timeoutMs/maxRetries/maxRetryDelayMs, AgentConfig.maxToolRounds alias, compaction-observational-memory pre-0.0.19 flat keys, read.ts transformImage flag, cli-init listInitProviders) — that is the 0.1.5 breaking cut with docs/migration.md removed-symbols list",
   "0.1.6 items: durable ACP session store, native sandbox backend, pdf/office reader, recursive delete, brace-expanding glob, loaded-skill body persistence",
   "0.1.7 items: prompt-cache telemetry, model-router cost/latency routing, async AgUiProjection hooks, prism providers add scaffold",
   "0.2.0 items: protected live-canary matrix, live NATS suite, new OpenAI-compatible model providers, prism-providers umbrella membership fix, delegated-agent adapters (Cursor/Antigravity), enterprise adapter breadth (Cedar/second object store/OpenAPI pagination), OTel spans for delegated agents",
   "new packages or new package subpaths",
   "new runtime or dev dependencies (esbuild/rollup/webpack/vite/tsup are explicitly rejected; the tree-shake bench stays zero-dependency; the dependency-free core and no-new-devDep promises hold)",
   "deletion or rewriting of ponytail: comments (intentional documented shortcuts moved verbatim with their code, not deleted)",
   "breaking changes to the frozen 0.1.x support matrix or public contract surface",
   "any root @arnilo/prism surface or behavior change from the CDP feature (root arnilo__prism.txt stays zero added/removed/changed; the feature lives in packages/browser only), and CDP domains beyond the agentic allowlist (Runtime/Network/DOM/Emulation): no cookies, tracing, performance profiles, IndexedDB, or worker/auto-attach debugging in 0.1.4; no browser launching or downloading; no new dependencies (playwright-core remains the existing optional peer)"
  ],
  "deviations": [
   {
    "task": "task2",
    "change": "the agents split exports 14 previously-private helpers from the split modules (RuntimeAgentSession, pendingDecisionsOf, resolveRunDecisions, decisionIdentityRef, decisionScopesEqual, nestedApprovalId, nestedOutcomeToolResult, pathsEqual, AgentRunSuspended, activeTools, policyList, toolElicitationRequest, validateElicitationPayload, throwIfAbortedSignal) so the modules can import each other; the union-surface walker (every dist/*.d.ts is a root) records them, so scripts/compat-baseline/arnilo__prism.txt gains 14 additive internal declarations at the Task 4 regeneration",
    "rationale": "unavoidable for any real module split (cross-module imports need exports); the package exports map exposes only '.' and the listed subpaths, so the names are not consumer-importable; release:gate fails only on removed/changed (added is non-breaking); the public entry surface (index.d.ts) is byte-identical with zero removed/changed and zero added at the entry"
   },
   {
    "task": "task4",
    "change": "scope amendment 2026-08-10: 0.1.4 adds the browser CDP agentic-capabilities feature to @arnilo/prism-browser (Tasks 4-5), extending the original god-module-split-only scope; @arnilo/prism-browser's compat baseline regenerates with added exports + optional-member interface extensions (zero removed) instead of remaining byte-identical",
    "rationale": "requested by the user; the root @arnilo/prism surface stays identical (zero added/removed/changed), the prism-browser deltas are additive and source-compatible (optional members), release:gate remains non-breaking, and the feature rides the existing optional playwright-core peer dep with zero new dependencies"
   }
  ]
 },
 "compat": {
  "baseline": "scripts/compat-baseline",
  "baselineRelease": "0.1.3",
  "promise": "identical PUBLIC declaration surface at the ROOT package entry (index.d.ts) vs scripts/compat-baseline across the 0.1.4 bump: zero added, zero removed, zero changed deltas at the root entry (the split moves declarations behind barrel re-exports so the entry .d.ts surface is byte-identical; only the version literal changes). The root union .d.ts baseline additionally gains 14 additive internal helper declarations from the split modules (deviation #1: non-public, not consumer-importable via the exports map, non-breaking per release:gate). Scope carve-out (deviation #2, browser CDP feature, Tasks 4-5): @arnilo/prism-browser's own baseline (arnilo__prism-browser.txt) is regenerated with the documented CDP additions only — new exports plus optional-member interface extensions on PlaywrightBrowser/PlaywrightBrowserContext, zero removed, source-compatible, additive-only per the 0.1.x promise. Stronger than the 0.1.x additive-only promise at the root entry."
 },
 "supportMatrix": "frozen at scripts/phase12-freeze-manifest.json (0.1.x compatibility and support matrix: Node, PostgreSQL, platform, provider, protocol pins, unsupported combinations); 0.1.4 changes none of it",
 "releasePolicy": {
  "auditLevelTarget": "moderate",
  "signedTag": "v0.1.4",
  "provenance": "npm OIDC trusted publishing plus GitHub build attestations on tarballs and SBOM (release.yml publish job)",
  "publication": "explicit operator action after evidence review; never automatic"
 },
 "tasks": {
  "$comment": "per-task evidence tokens; 'done' when the task passes its checks, 'pending' until then. Task 0 sets itself done at freeze; Tasks 1-6 move to done as they land. Tasks 4-5 are the browser CDP scope amendment (2026-08-10); Task 6 is the exit gate (renumbered from the original task4).",
  "task0": "done (freeze record + scope gate + baseline evidence at scripts/phase16-baseline.json; pre-split snapshot: dist/agents.js 111049 bytes, dist/contracts.js 9420 bytes, dist/agents.d.ts 908 bytes, dist/contracts.d.ts 98825 bytes, dist 64 js + 64 d.ts modules, reachability from agents.js 32/64, src/agents.ts 2576 lines/4 exports, src/contracts.ts 2549 lines/295 exports; npm test core 1425/1425 gates 134/134 workspaces green; audit 0 moderate; release:gate 0.1.3 49 packages 0 breaking deltas; package.json test script wired with scripts/phase16-freeze.test.mjs)",
  "task1": "done (contracts.ts split into contracts-core.ts (1695 lines: JSON/content/messages/models/limits/guardrails/cache/provider-request/AIProvider/realtime-session/agent-config/commands/context/instructions/skills/extensions/session-stores/persistence/run-feedback/queries/compaction/retry/resource/credential/loop/artifact) + contracts-run-state.ts (341 lines: AgentRunStatus..AgentSession run-state/approval/decision contracts + decision/steer limit constants) + contracts-protocol.ts (577 lines: ProviderEvent/RealtimeEvent/InputAssemblyLayout+RunOptions/ProviderTurnMetadata/ToolExecutionMetadata/AgentEvent/ToolEffect/Record/RunLedger family/ProviderTurnResult) behind the contracts.ts barrel (18 lines) keeping the 7 implementer alias re-exports; union .d.ts surface byte-identical vs scripts/compat-baseline at 0.1.3: 0 added / 0 removed / 0 changed (702 = 702 names), checked via release:gate and direct diffSurface at the current 0.1.3 version (Task 4 re-runs at 0.1.4); cross-module edges all import type (zero runtime cycles; contracts-protocol.js type-only at 57 bytes); dist/contracts.js 9420 -> 418 bytes, dist/contracts.d.ts 98825 -> 1029 bytes, dist module count 64 -> 67; npm test exit 0 unchanged in count/outcome: core 1425/1425, script gates 150/150, workspaces green; file-coupled surface tests (core-boundaries.test.ts, docs.test.ts, public-export-contract.test.ts, scripts/phase15-freeze.test.mjs, phase16-freeze.test.mjs barrel-intent) updated to read the split-module union, assertions unchanged; no new deps; deviations empty)",
  "task2": "done (src/agents.ts (2576 lines) split into src/agent-session.ts (2020 lines: RuntimeAgentSession, EventSubscriber, createAgent/createAgentSession, shared helpers) + src/agent-approval.ts (236 lines: pendingDecisionsOf/resolveRunDecisions/validateModifiedArguments/AgentRunSuspended/nestedApprovalId/pathsEqual/decisionScopesEqual/nestedOutcomeToolResult/decisionIdentityRef/ActiveDurableRun) + src/agent-tool-dispatch.ts (115 lines: toolElicitationRequest/validateElicitationPayload/safeToolElicitationValidate/activeTools/policyList) + src/agent-run-lifecycle.ts extended in place (356 lines: resumeAgentRun/resumeAgentRunStream/prepareAgentRunResume/executePreparedAgentRunResume; plan-preferred option, no new file) behind a 16-line agents.ts barrel with NAMED re-exports (deliberately not star: star would surface internal helpers through agents.d.ts); fingerprint reused from the existing agent-run-state.ts (no new module); tsc 0 diagnostics; npm test exit 0 core 1425/1425 unchanged, script gates 151/151 (17 phase16 incl. tree-shake leg), workspaces green; dist/agents.js 111049 -> 982 bytes, dist module count 64 -> 70 js/70 dts; release:gate 0 removed/0 changed, 14 added internal-only helper declarations logged as deviation #1 (cross-module imports need exports; exports map exposes only '.' + listed subpaths so not consumer-importable; added is non-breaking); no class split, no behavior change, no new deps)",
  "task3": "done (scripts/phase16-tree-shake.mjs zero-dep bench: stat dist/agents.js + dist/contracts.js bytes, dist/*.js + dist/*.d.ts module counts, static-import BFS reachability from agents.js/contracts.js; writes the treeShake block into scripts/phase16-baseline.json with shrink/rise/no-worse deltas vs the Task 0 splitBaseline; ponytail: static-reachability ceiling comment with 0.1.7 esbuild-bundle upgrade path; freeze-test leg runs the bench (timeout 5s) and gates: agents.js 982 < 111049, contracts.js 418 < 9420, module count 70 > 64, reachability recorded not gated (39 from agents.js vs 32 pre-split; 4 vs 1 from contracts.js); docs/performance.md 0.1.4 tree-shake section; no new deps)",
  "task4": "done (browser CDP implementation landed in packages/browser: src/cdp.ts (structural PlaywrightCdpSession + per-run page-session creation + Runtime/Network/Emulation domain helpers; cdpAvailable signal = context.newCDPSession function presence + non-firefox/webkit version), src/evaluate.ts (bounded Runtime.evaluate: expression <= maxActionInputBytes, timeout clamped to actionTimeoutMs, returnByValue, JSON result capped at maxEvaluateResultBytes with truncation flag, bounded exception descriptions), src/observe.ts (per-page console/network rings with drain-on-read, caps maxConsoleEntries/maxNetworkRequests, eviction + truncated flag, NEVER captures bodies/cookies/auth headers, argument previews bounded 512B x4), src/types.ts (PlaywrightCdpSession, BrowserCdpOptions mode auto/on/off, optional newCDPSession?/newBrowserCDPSession? members, 4 new act actions block_urls/unblock_urls/throttle/emulate, css/xpath targets, BrowserEvaluateRequest/Result, CdpConsoleEntry/CdpNetworkEntry, BrowserObserveResult), src/manager.ts (BrowserManager.evaluate/observe methods serialized through the run queue; per-page CDP state with mode gating (off -> ERR_PRISM_BROWSER_CDP_UNAVAILABLE) and Chromium-capability check; act actions block_urls (patterns <= maxBlockedUrlPatterns)/unblock_urls/throttle (latency <= 120000, kbps <= 1000000, reset -> CDP -1 throughput)/emulate (dimensions <= 16384, scale <= 10, optional userAgent, reset -> clearDeviceMetricsOverride); evaluate charges action budget + beforeSideEffect hook; CDP sessions detached on closeRun/browser_close (run-scoped reset)), src/tools.ts (browser_evaluate + browser_observe tools, new act params, css/xpath targets, updated descriptions), src/policy.ts (evaluate high_impact/high/side-effect-hook; block/throttle/emulate mutation/medium; observe observation; isSideEffectAction widened to cover evaluate/observe), src/limits.ts (DEFAULT/HARD maxEvaluateResultBytes 64K/256K, maxConsoleEntries 200/500, maxBlockedUrlPatterns 32/128 + 5 hard throttle/emulate caps), src/errors.ts (ERR_PRISM_BROWSER_CDP_UNAVAILABLE + ERR_PRISM_BROWSER_CDP), src/targets.ts (css/xpath targets resolve via Playwright selector engine), src/index.ts additive re-exports, README CDP section; fakes: FakeCdpSession + newCDPSession on FakeContext/FakeBrowser + css/xpath fake locators. Test-expectation updates required by the feature: browser.test.ts normalizeTarget (css/xpath accepted, selector/evaluate keys still denied) + six-tools assertion; eval-fixtures.test.ts css-evaluate-rejected item now asserts the 0.1.4 surface. Verification: tsc 0 diagnostics, browser workspace tests 40/40, 5-section network-free smoke against dist (evaluate value/exception/bounds/truncation; observe drain/caps/eviction/no-bodies; act CDP params + reset + detach on closeRun; policy hook for evaluate/block_urls + CDP_UNAVAILABLE for mode off and missing newCDPSession; six-tool surface) all pass, full npm test exit 0 core 1425/1425 + gates 151/151 + workspaces green. release:gate at 0.1.3: root @arnilo/prism 0 deltas; @arnilo/prism-browser +41 added (new exports + FakeCdpSession dist-test-support, same non-consumer-importable pattern as deviation #1) / 0 removed / 18 changed (15 = re-export-statement text artifacts from the shared types statement growing; 3 semantic: optional newCDPSession?/newBrowserCDPSession? members on PlaywrightBrowser/PlaywrightBrowserContext + isSideEffectAction param widening — all source-compatible) — exactly the deviation #2 carve-out; prism-browser baseline regeneration deferred to Task 6. Zero new dependencies; biome format applied)",
  "task5": "done (browser CDP tests/docs landed: new packages/browser/src/__tests__/cdp.test.ts (15 tests: cdp session layer — resolveCdpMode/cdpAvailable/createPageCdpSession errors/domain helpers; act actions — block_urls/unblock_urls/throttle/emulate param validation + reset + run-scoped detach on closeRun/close; css/xpath target resolution; mode-off CDP_UNAVAILABLE while Playwright-only tools keep working; policy classification table evaluate high_impact/high + 4 CDP act actions mutation/medium + observe observation; new limits default/hard pairs + RangeError validation), evaluate.test.ts (13 tests: evaluateInPage value round-trip + Runtime.evaluate params incl awaitPromise/userGesture/returnByValue/timeout, empty/oversize expression limits, timeout bounds, bounded exception descriptions with location, result truncation at maxEvaluateResultBytes, CDP send failure -> ERR_PRISM_BROWSER_CDP; boundedJson round-trip/truncation/non-stringifiable degradation; manager.evaluate end-to-end + beforeSideEffect hook + mode-off + browser_evaluate tool untrusted_external trust), observe.test.ts (10 tests: ring drain semantics + seq ids + cap eviction + sticky truncated; installCdpObservation console/exception/network mapping, BODY/COOKIE/AUTH never-captured invariant, arg-count/preview-bytes bounds, unsubscribe; manager.observe Runtime+Network enable-once + drain-on-read + maxConsoleEntries cap + mode-off + no side-effect hook)); live.test.ts extended with a real-Chromium CDP leg (evaluate document.title + exception, observe network entries across navigation, css/xpath targets, emulate devicePixelRatio=3) gated by PRISM_LIVE_PLAYWRIGHT=1 — verified 4/4 against installed chromium-1228; the stale pre-CDP live assertion (normalizeTarget css rejected) was updated to the 0.1.4 surface. Docs: docs/browser-automation.md CDP section (six tools, capability gating, evaluate/observe bounds, block/throttle/emulate semantics, security: policy gate/no body capture/run-scoped reset/domain allowlist/CDP-not-an-egress-bypass), docs/index.md browser line names browser_evaluate/browser_observe + CDP act actions, packages/browser/README.md CDP section (landed in Task 4). Biome format applied. npm test exit 0: core 1425/1425, script gates 151/151, browser workspace 78/78 (40 prior + 38 new). release:gate diff at 0.1.3: root arnilo__prism.txt 14 added (deviation #1, baseline regen at Task 6) / 0 removed / 0 changed; arnilo__prism-browser.txt 41 added / 0 removed / 18 changed (15 re-export-statement text artifacts + 3 semantic optional-member/signature-widening) — exactly the deviation #2 carve-out. Zero new dependencies.",
  "task6": "done (exit gate: scripted bump 0.1.3 -> 0.1.4 across all 49 manifests + lockfile via node scripts/release.mjs bump (lockfile name-set unchanged — zero new deps, sha256 recorded); version pins updated (src/index.ts version const, index.test.ts, release.test.ts, install-smoke.test.ts tarball names, packaging.test.ts peers/deps, docs.test.ts root-manifest + current-line + new plan 016 Task 6 tripwire, 12 workspace index.test.ts peer pins); docs reconciled (CHANGELOG 0.1.4 entry, docs/migration.md 0.1.3 -> 0.1.4 no-migration section with the 0.1.5 forward reference, docs/index.md current **0.1.4** line, docs/release-and-install.md 0.1.4 publish handoff, agent-session-runtime.md + public-contracts.md internal-file-structure notes); compat baselines regenerated via release:gate --version 0.1.4 --update-baseline (root arnilo__prism.txt 702 + 14 deviation-#1 additive internal helpers = 716 lines, 0 removed/0 changed; arnilo__prism-browser.txt 153 + 41 documented CDP additions = 194 lines, 0 removed — deviation #2 carve-out) and re-gated clean (0 errors, 49 packages); exit gate green and recorded in scripts/phase16-baseline.json exitGate: npm test exit 0 (core 1426/1426, script gates 153/153 incl. the two new exit-gate legs), sdk:ready rc 0, npm audit --audit-level=moderate 0 vulnerabilities, pack dry-run 49/49 twice byte-identical (sha256 d20ac85b...), release:gate 0.1.4 0 errors, benchmark evidence unchanged (scripts/benchmark-0.1.0.json sha256 recorded), lockfile name-set hash recorded; biome format applied; publication remains the operator handoff (docs/release-and-install.md 0.1.4 publish handoff: signed v0.1.4 tag + npm OIDC)."
 },
 "security": {
  "blockedGatePolicy": "missing credentials or infrastructure records a named blocked release gate, never a passing skip (inherited from scripts/phase12-freeze-manifest.json)",
  "auditPolicy": "npm audit --audit-level=moderate stays at 0 moderate-or-higher findings at 0.1.4",
  "splitPolicy": "the god-module split changes no trust boundary, no credential, no network call, no persistence shape, and no runtime behavior; the approval/pending-decision, tool-dispatch, and resume code (the security-critical paths) move verbatim — the existing adversarial suites (security:threat-suites, approval idempotency, cross-tenant isolation) pass unchanged"
 }
}