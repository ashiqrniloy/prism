# 0.8.0 cut evidence

Ledger for plan 085 Tasks 7–8. Not a current-contract API page. **0.8.0 is the seven-plan line**: 079, 080 Tasks 1–9, 081, 082, 083, 084, and 085 Tasks 1–6. **080 Task 10 is superseded by 085 Tasks 7–8.** Plan 079 is **in** this cut (it was `out` of 0.7.0).

Machine copy: [`0.8.0-cut.json`](./0.8.0-cut.json).

## Inventory

| Plan | Surface in this cut |
| --- | --- |
| 079 | `@arnilo/prism-channels`: transport-neutral runtime/journal, Telegram, experimental Signal, durable approvals, restart/recovery |
| 080 Tasks 1–9 | Telegram group/topic grants, drafts, bounded media/voice, opt-in notices, ERP outbox composition, SIGTERM/soak, nested test glob, resume signal, checkpoint scope miss |
| 081 | Identity-bound connected-app MCP session, GWS/M365 HTTP adapters, Slack MCP wrap, sidecar example |
| 082 | Package-truth evidence generation and connected-app follow-up safety review |
| 083 | `@arnilo/prism-office` → `@arnilo/prism-work` (only planned breaking import-map change), work sandbox, vendored skills |
| 084 | Durable turn checkpoints/`continue`, turn-stop policy, AG-UI input policy, run bundle, claim grounding, typed provider failures, tool-call strictness |
| 085 Tasks 1–6 | This-tree Postgres evidence, coverage names, wiki `--test-isolation=none`, OM worker tool-only + typed errors, fail-closed lease release, private runtime split, provider cleanup, `retryableAdmission` / work-tool registrar |

Eleven publishable manifests at exact **0.8.0** with internal caret ranges `^0.8.0`. Predecessor published line: **0.7.0**.

## Compat baseline

Task 7 regenerated `scripts/compat-baseline/*`. **085 Tasks 1–6 add no public removals.** Inherited 083 `@arnilo/prism-office` → `@arnilo/prism-work` removals were already in the 084 baseline regen (zero removals vs pre-cut baselines). 085 additions (dist-wide declaration scan, not package.json exports): `@arnilo/prism-channels` +14, `@arnilo/prism-work` +14. `@arnilo/prism` `version` 0.7.0 → 0.8.0.

## Verification (Task 8, 2026-09-18)

Recorded against `git rev-parse HEAD` `a7915d6c2c79f2fbfdc61f290301b918b54e1025` plus the 0.8.0 working tree. Registry/tag writes stay out.

- `npm test` 6/6 stages (build, performance budget, root, gate, build race, workspace)
- `npm run typecheck` / `lint` / `format:check` green
- coverage core 92.50 / 85.85 / 93.07 against 60/70/75; artifact keys include `@arnilo/prism-work`, not `@arnilo/prism-office`
- `npm run pack:dry-run` green for all 11 packages
- `PRISM_TEST_POSTGRES_URL=… npm run test:postgres` — 544 tests, 540 pass, 0 fail; `scripts/postgres-evidence.json` `gitHead` matches HEAD
- `node scripts/drill-migration-rollback.mjs --url …` ok (apply → downgrade 009 → verify-compat → re-apply → checksum-fail-closed + SQLite); `--self-test` URL refusals ok
- `PRISM_TEST_POSTGRES_URL=… npm run release:gate` — 43 surfaces, 12 pass, 31 protected, `blocked: false`; postgres row is `pass` with count 544
- `npm run security:threat-suites` 83/83
- `npm audit --audit-level=moderate` 0
- secret scan 6895 files, 0 findings (`live.env` skipped by name)
- SBOM regenerated (173 packages) and `scripts/verify-sbom.mjs` clean
- `npm run release:check -- --lockstep --version 0.8.0 --allow-dirty --allow-untagged` 11/11 available
- `npm run release:publish -- --dry-run --lockstep --version 0.8.0 --allow-dirty --allow-untagged --skip-tarball` 11 dry-run packs including `@arnilo/prism-channels`
