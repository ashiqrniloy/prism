# Phase 111 — Reranker recall and latency on a semantic embedder (generated)

Generated by `packages/memory/src/rag/__tests__/local-reranker-live.test.ts` on a gated run
(`PRISM_TEST_LOCAL_RERANK=1`). Re-run the leg to regenerate; never hand-edit the numbers.

| field | value |
| --- | --- |
| reranker | `Xenova/bge-reranker-base` |
| reranker dtype / device | `q8` / `cpu` |
| machine | arn-cachyos (AMD Ryzen 9 PRO 7940HS w/ Radeon 780M Graphics) |
| platform | linux x64, Node v26.9.0 |
| reranker weights | cache hit (282.7 MB in cache before load) |
| reranker lazy load | 1 load, 1261 ms |
| conformance | pass (`runRerankerConformance`: permutation, deterministic, empty→empty) |
| ordering | `src#0001` ranked first for "capital of France" (exact answer beat the distractor) |
| top-50 latency | median 95 ms over 5 runs (min 92, max 110); one warm-up run excluded |
| recall corpus | 24 queries / 96 chunks (one answer + three mention-only chunks per topic), `lexical: "off"`, vector-only, whole-corpus pool, k=5 |
| baseline embedder | `prism-hash-embedder` (32 dims), index build 1 ms |
| semantic embedder | `Xenova/all-MiniLM-L6-v2` q8/cpu (384 dims), load 358 ms, index build 154 ms |
| recall@5 (hash) | 0.208 → 0.792 (vector order → `Xenova/bge-reranker-base` q8/cpu) |
| recall@5 (semantic) | 0.792 → 0.792 (vector order → `Xenova/bge-reranker-base` q8/cpu; identity control 0.792 → 0.792) |
| candidate-pool bound (hash) | recall@20 at a 20-candidate pool = 0.625; recall@32 at a 32-candidate pool = 0.792 |
| candidate-pool bound (semantic) | recall@20 at a 20-candidate pool = 1.000; recall@32 at a 32-candidate pool = 1.000 |
| recall misses (hash) | baseline: propagation-delete, rerank-order, invalidation-stale, chunking, embedder-dimensions, lineage-layer, retention-expiry, redaction, telemetry-span, sandbox-scope, approval-side-effect, trust-untrusted, fusion-rrf, result-cap, wiki-raw-source, grant-sharing, audit-log, om-ledger, conformance | after rerank: telemetry-span, sandbox-scope, wiki-raw-source, audit-log, conformance |
| recall misses (semantic) | baseline: sandbox-scope, fusion-rrf, result-cap, wiki-raw-source, audit-log | after rerank: telemetry-span, sandbox-scope, wiki-raw-source, audit-log, conformance |
| weight cache layout | one host cache dir with `Xenova/bge-reranker-base` and `Xenova/all-MiniLM-L6-v2` subdirectories (the documented convention) |
| offline replay | pass (`allowRemoteModels: false` reranker + `local_files_only: true` embedder shared the cache: zero network after load) |
| non-CPU leg | not run: fp16/GPU is host-provisioned — re-measure before expecting these CPU numbers to hold |
| secrets / paths | none: no credentials, no document text, no absolute host path |

Latency and recall are evidence, not gates: hardware, dtype, and the embedder move them. With this
semantic embedder the baseline starts at the hash leg's reranked number and the reranker's lift is
**0.000**: at the package default (`DEFAULT_QUERY_CANDIDATES = 20`) the semantic pool bound is
1.000, so the pool is not the binding constraint and the remaining misses are
retrieval-quality misses neither leg fixes. Read the pool-bound row for your own embedder before sizing the
reranker; the hash row set in [phase102-local-rerank-latency.md](phase102-local-rerank-latency.md) stays the
lexical control. Model weights come from the documented model ids into the host cache dir; nothing is declared
in any manifest (`@huggingface/transformers` stays a host seam) and the run requires no credentials.
