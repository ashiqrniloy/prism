# 0.7.0 host-completeness evidence

Ledger for plan 073 Task 27, closed by Task 29. Not a current-contract API page. **0.7.0 shipped without an interim cut**, and the cut now covers the six-plan line that actually landed: 072, 073, 074, 075 ([Memory Fabric](../../plans/075-Memory-Fabric.md), reassigned from 0.8.0 on 2026-09-14), 077 and 078. **Plan 079 (Telegram/Signal channels) moved to 0.8.0 on 2026-09-15 by user request** — the cut no longer waits on it, and no channel adapter ships here.

Machine copy: [`0.7.0-host-completeness.json`](./0.7.0-host-completeness.json).

Status vocabulary: `passed` (hermetic proof ran), `environment-blocked` (proof exists but needs credentials/infrastructure), `blocked` (owning plan still open), `out` (removed from the 0.7.0 ledger).

## Status

| id | status | tier | proof |
| --- | --- | --- | --- |
| Trap A | passed | hermetic | ACP MCP origin/path allow-list tests |
| Trap B | passed | hermetic | router `providerSource` refuses unenforceable governance |
| Trap C | passed | hermetic | ACP real model/provider pairing tests |
| R01 | passed | hermetic | model-router admission + `recordUsage` |
| R02 | passed | hermetic | work-draft revision bind + packed stale-approval reject |
| R03 | environment-blocked | live | Docker sandbox tests pass; E2B/Docker live legs need credentials |
| R04 | environment-blocked | live | RAG ACL tests pass; Drive/postgres live legs need credentials |
| R05 | passed | hermetic | host composition + packed personal/business compositions |
| R06 | passed | hermetic | workflow coordinator admission |
| R07 | passed | hermetic | `trials.sampleCount`, denyTools/unknownEffect, release manifest, coding oracle |
| R08 | passed | hermetic | memory correct/forget + revoked-id absent |
| R09 | out | — | removed 2026-09-14 |
| R10 | environment-blocked | live | import fidelity + citation integrity pass; Mistral OCR live leg needs credentials |
| R11 | passed | hermetic | per-run `toolNames` narrowing + durable restart |
| R12 | environment-blocked | live | Bedrock native `converse` route shipped (Task 22; hermetic frame/body fixtures); Vertex half (Task 23) removed; live IAM/IRSA probe not run |
| R13 | out | — | removed 2026-09-14 |
| R14 | passed | hermetic | realtime barge-in cancels the effect before execution |
| R15 | passed | hermetic | timeline folder + workflow graph Mermaid |
| R16 | passed | hermetic | [plan 077 evidence](phase77-primitive-review.md), work-scope unit tests, and packed public-surface journey prove open/bind/project/enter, the projected compaction pack, and closed-scope reflection promotion |
| R17 | passed | hermetic | [plan 074 evidence](phase74-attention-measurements.md), the attention compiler suites (contracts, stages, wiring, events, follow-ups), and the packed journey leg that arms a truncated streak, compacts once, and consumes it |
| 075 / 078 | passed | hermetic | no review id: [Memory Fabric](../../plans/075-Memory-Fabric.md) and [host-owned spawn](../../plans/078-Host-Owned-Subagent-Spawn-And-Parallel-Agents.md) entered the 0.7.0 line on 2026-09-14; their plan artifacts, suites, and packed journey legs (`R16 OK`, spawn/lifecycle tests) are the linked evidence |
| 079 | out | — | [messaging channels](../../plans/079-Prism-Messaging-Channels-Telegram-Signal.md) reassigned to **0.8.0** on 2026-09-15; not part of this cut |

## Packed proof

`scripts/fixtures/e2e-070-host-completeness-journey.mjs` runs inside the full-surface packed consumer (`scripts/e2e-full-surface.test.mjs`) using public exports only and prints one line per leg with elapsed ms. Its R16 leg proves task-local memory stays hidden from task 15 until an explicit ancestor bind promotes it, and that a reflection bound to the closed task scope promotes into a `procedure` note with `promotedFrom` provenance through the packed fabric subpath; its R17 leg proves the packed attention compiler and the host-programmable truncation trigger (arming, one compaction per streak, streak consumption, threshold validation). Other legs cover ACP MCP allow-list, personal + business composition readiness, stale draft approval, memory correction + revoke, per-run tool grant, interrupted-run restart (`resumeAgentRun` executes the gated write exactly once), eval trials (`sampleCount === 3`), `denyTools`, `unknownEffect`, coding test oracle, citation integrity (revoked ACL), timeline summary, workflow graph, and voice barge-in before effect.

Negative controls (`scripts/host-completeness-evidence.test.mjs`): every ledger row present exactly once, out-of-release rows cannot be claimed passed, blocked rows cannot be claimed passed, every named test path exists, every declared live suite is `active` in `scripts/live-matrix.json`, and removing a required live suite fails the check.

Live suites required to exist as `active` (credentials still skip-not-fail): `core/postgres`, `memory/postgres`, `coding-tools/docker-sandbox`, `web-tools/browser-live`, `memory/drive-sync-live`, `coding-tools/e2b-sandbox-live`, `work/mistral-ocr-live`.

## Release rule

`passed` on a row that claims a live integration is a release fail: those rows stay `environment-blocked` until an approved disposable environment runs them. R16 points to plan 077's hermetic and packed proof; R17 to plan 074's suites plus the packed journey leg. R09/R13 stay `out`, and R12 stays `environment-blocked` until its protected Bedrock IAM/IRSA probe runs (its Vertex half remains out via Task 23). Task 29 must not ship with any `blocked`, `out`-that-is-claimed, or live-claiming `passed` row.

**Protected PostgreSQL legs re-verified on the 0.7.0 tree** (`PRISM_TEST_POSTGRES_URL=… npm run test:postgres` against `pgvector/pgvector:pg16`, the image the release workflow uses): core 72/72, memory 457/457, phase conformance 11/11. That leg found and closed three real defects the hermetic doubles could not see — a `42P08` parameter-type ambiguity on the first task-scoped budget insert, a swallowed serialization failure that issued SQL against an aborted transaction, and a retry policy too small for 16 concurrent serializable writers.
