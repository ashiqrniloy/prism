# Phase 102 — Local reranker live leg (generated)

Generated by `packages/memory/src/rag/__tests__/local-reranker-live.test.ts` on a gated run
(`PRISM_TEST_LOCAL_RERANK=1`). Re-run the leg to regenerate; never hand-edit the numbers.

| field | value |
| --- | --- |
| model | `Xenova/bge-reranker-base` |
| dtype / device | `q8` / `cpu` |
| machine | arn-cachyos (AMD Ryzen 9 PRO 7940HS w/ Radeon 780M Graphics) |
| platform | linux x64, Node v26.9.0 |
| weights | cache hit (282.7 MB in cache before load) |
| lazy load | 1 load, 1261 ms |
| conformance | pass (`runRerankerConformance`: permutation, deterministic, empty→empty) |
| ordering | `src#0001` ranked first for "capital of France" (exact answer beat the distractor) |
| top-50 latency | median 95 ms over 5 runs (min 92, max 110); one warm-up run excluded |
| recall corpus | 24 queries / 96 chunks (one answer + three mention-only chunks per topic), lexical `prism-hash-embedder` baseline, `lexical: "off"`, vector-only |
| recall@5 (pool = whole corpus, 96 candidates) | 0.208 → 0.792 (vector order → `Xenova/bge-reranker-base` q8/cpu) |
| candidate-pool bound | recall@20 at a 20-candidate pool = 0.625; recall@32 at a 32-candidate pool = 0.792 — reranking cannot recover an answer the pool never returned |
| recall misses | baseline: propagation-delete, rerank-order, invalidation-stale, chunking, embedder-dimensions, lineage-layer, retention-expiry, redaction, telemetry-span, sandbox-scope, approval-side-effect, trust-untrusted, fusion-rrf, result-cap, wiki-raw-source, grant-sharing, audit-log, om-ledger, conformance | after rerank: telemetry-span, sandbox-scope, wiki-raw-source, audit-log, conformance |
| weight cache layout | one host cache dir with a `Xenova/bge-reranker-base` subdirectory (the documented convention; the same dir is reusable by local embedders) |
| offline replay | pass (`allowRemoteModels: false` shared the cache: zero network after load) |
| secrets / paths | none: no credentials, no document text, no absolute host path |

Latency and recall are evidence, not gates: hardware, dtype, and the embedder move them, so the leg
records them and the strict live run proves the seam. Both measured legs score the whole corpus, so
the recall delta is ordering only; the pool-bound row is why `queryCandidates` matters more than the
reranker when recall is short. Model weights come from the documented model id into the
host cache dir; nothing is declared in any manifest (`@huggingface/transformers` stays a host
seam) and the run requires no credentials.
