# Orientation

Fixture repository overview (network-free demo graph):

- `src/input.ts` — provider input assembly and context flattening.
- `src/middleware.ts` — extension middleware registry (`tool_result`, `context`).
- `src/context-budget.ts` — context budget demotion for injected blocks.
