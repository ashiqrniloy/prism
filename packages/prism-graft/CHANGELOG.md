# Changelog

All notable changes to `@arnilo/prism-graft` will be documented in this file.

## [0.0.1] - Unreleased

### Added
- Initial release: `createGraftExtension` with fail-closed graft CLI resolution (`cliPath` / `packageRoot` / optional peer `@nanonets/graft@^0.13.0`), six pull tools (`graft_ask`/`grep`/`callers`/`skeleton`/`map`/`blast`), push-mode retrieval-pack context provider + first-turn orientation injector, post-edit blast-radius middleware with `graft:dirty`, session `graft-state` persistence, budgeted JSON runner exports (`runGraftJson`, `childEnv`). Plan 033.

## [0.1.0] - 2026-08-09

### Added
- Initial package declaration for `@arnilo/prism-graft`.

## [0.0.28] - 2026-08-08

### Added
- Reserved package entry for the graft integration.
