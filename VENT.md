# VENT

Feedback log. Repeated/systemic workflow friction that should become future automation, docs, or workflow fixes.

## 26-08-15 05:48 — budget-gate regression from Task 1 splits missed during Task 1 verification, caught in Task 3

Task 1's verification footer claimed "npm test → exactly 1 failure (roadmap)" but the budget-gate was ALSO failing (root fileCount 326→356 from the god-module splits adding dist/contracts-core/ + dist/agent-session/ .js/.d.ts files). This was only caught during Task 3 when I ran the budget-gate directly. Root cause: grepping `npm test` output for `^✖` to summarize pass/fail can miss a budget-assertion failure if the summary line is misread or the gate test's failure isn't surfaced prominently. Fix: after any change that ADDS dist files (splits, new modules), explicitly run `node --test scripts/budget-gate.test.mjs` and re-baseline the over-tolerance metric per the release convention BEFORE declaring the task verified. The Task 6 exit gate re-checks the budget, but catching it at the source task (1) avoids carrying a red gate through Tasks 2-5.
## 26-08-15 06:35 — npm test && chain + a deferred early failure hid 3 freeze-test regressions + lint diagnostics across Tasks 1-4

The `npm test` script chains stages with `&&` (dist/__tests__ → script-gates → workspace tests). A single early failure stops the chain, HIDING all downstream failures. The `roadmap carries no stray/truncated content` failure (from a user roadmap.md edit) persisted across Tasks 1-4 as "pre-existing, out of scope" — but it blocked the `&&` chain, hiding 3 Task-1 freeze-test regressions (phase15/16/17 broken by the god-module splits) + biome lint diagnostics in the phase25-* helpers. These only surfaced in Task 5 when the coverage gate (test:coverage) needed the chain to run to completion. Lesson: a "pre-existing" failure that blocks a `&&` chain is not cosmetic — it masks real regressions. Fix blocking early failures immediately (or restructure the test script to run all stages + report every failure, not `&&`-short-circuit), and never declare a task "verified" while a known failure blocks the chain — run the later stages explicitly (`node --test scripts/phase15-freeze.test.mjs ...`, `npx biome lint .`) to confirm they're green, not just absent from the truncated output.
## 26-08-17 18:27 — edit-tool-json-mangling

The edit tool repeatedly rejected edits with "Validation failed: edits.0: must be object" when newText/oldText contained arrow functions `=>`, regex with `>=`, or unicode escapes like `\u003e`. The JSON the harness receives appears to get mangled on certain character sequences (the `>` in `=>` and `>=` especially). Workaround used 5+ times this session: fall back to `python3 - <<'PYEOF'` with open(p).read()/replace()/write() for any edit containing `=>`, `>=`, or `\u` sequences. Fix suggestion: either the edit tool should accept the raw object array reliably, or document that `>`-bearing code must be edited via python/bash heredoc. This forced context-switching on nearly every multi-line code edit in the journey script.
## 26-08-20 15:15 — historical-freeze-count-drift

Adding one workspace package made nine historical phase13–21 freeze tests fail because each compared frozen workspace counts directly to current packages/*; repeated workaround added an explicit future-package exclusion to every file. Prevent with one shared historical-graph helper or phase-aware baseline comparison so additive releases do not require nine manual edits. Combined coverage-summary also reports pre-existing below-threshold packages even when new package coverage passes, so package-local evidence is needed until thresholds are refreshed.
## 26-08-20 21:08 — agy workspace MCP permission mismatch

Plan 031 Task 1 live proof initially assumed workspace-local MCP permission/config would be sufficient. `agy` 1.1.16 discovered the workspace server after `--add-dir`, but headless auto-denied `prism/prism_echo` because permissions are global at ~/.gemini/antigravity-cli/settings.json; `--dangerously-skip-permissions` is forbidden and global mutation is out of scope. Repeated probe retries were needed to distinguish wrapper-shape mismatch, path containment, and permission denial. Prevent with a documented host-owned narrow-permission preflight or a supported per-run permission override before live-proof tasks.
## 26-08-29 20:59 — npm publish via 30 package tags: no events (>3 tags/push), then E409 races

Two release-pipeline landmines cost ~30min during the plan-039 npm publish: (1) GitHub Actions silently drops workflow events when a single git push contains more than 3 tags — must push package tags in batches of ≤3; (2) firing all 30 package-tag publish runs concurrently makes them race each other, losers E409 on 'previously staged version' until registry consistency settles, requiring rerun waves. Next multi-package cut (plans 040+): script the tag push as batched (≤3) and consider staggering or a single dispatcher run instead of 30 parallel full-gate publishes.
## 26-08-30 15:00 — bash cwd resets between calls, ~10 retries per subtask

Shell cwd resets to the project root between many bash invocations in this harness (inconsistently), which repeatedly caused "No such file or directory" and stale-cwd mistakes when working inside packages/prism-dev. Workaround used repeatedly: prefix every command with `(cd <dir> && …)`. Would prevent backtracking if cwd persisted per-session or if commands always executed in an explicit, stable directory.
## 26-08-31 06:08 — graft grep CLI

Graft grep CLI rejected scoped/multi-pattern invocations with misleading `no graph` or `too many arguments`; repeated workaround was rerun as one bare literal search, then use raw rg only after graph context. Improve grep argument parsing/help to accept `--in` and multiple patterns consistently.
## 26-08-31 13:40 — missing plan references

Create-plan skill references `references/default.md` and `references/prism.md`, but both files are absent; repeated workaround was record ENOENT and apply the available `prism-wiki.md` rules. Add the missing defaults/project reference or make the skill loader declare them optional.
## 26-09-01 17:17 — stale dist test binaries + 10-file count-delta duplication during package consolidation

Repeated friction while executing plan-054 package moves: (1) editing src/__tests__/*.ts then running `node --test dist/__tests__/*.test.js` uses the stale compiled binary unless `npm run build:core` runs first — caused three rounds of confusing failures that looked like logic errors; a `test:dist` script that rebuilds-then-tests, or documenting the rebuild step in the plan approach notes, would prevent this. (2) The freeze-test count-delta pattern (`hasCodingTools ? -N`) is duplicated across 10 files (phase13-21, 24, 27, 29, 30, 34, benchmark-multi-agent, docs.test, release.test); every absorbed package requires touching all of them — a single shared expected-counts helper in package-truth.mjs would reduce each future consolidation task (5, 6) to one edit.
## 26-09-01 20:55 — repeated count-delta/config-floor fallout across plan 054 package moves

Plan 054 consolidation tasks keep tripping on the same three hidden coupling points, now confirmed across Tasks 2-5: (1) freeze-test count deltas duplicated across ~10 scripts each need recomputation per absorbed package — Task 5 required the same three-line sed across phase13-21 plus phase24/27/29/30/34/benchmark-multi-agent; (2) sweep-unused config-count floors (>=30) and docs canonical-count asserts embed historical package counts that silently break on every package removal; (3) phase17-baseline.json embeds absolute file paths of moved sources. A pre-task checklist in the plan ("counts to recompute: phase13-21 deltas, phase24 secondPeers+pureManifest list, phase23/sweep config floors, coverage thresholds, phase17 baseline paths, phase54 evidence regen") would make each remaining consolidation task mechanical.
## 26-09-01 21:54 — release-skip-manifest protected-class derivation broke on providers family merge

Task 6 fallout pattern (consistent with Tasks 2-5, third occurrence): release-skip-manifest.mjs derives live-provider-protected surfaces from per-package src scans — any family conversion that merges N packages into 1 silently drops N-1 protected classes and only the >=8 floor assertion catches it. Same class of bug as the coverage-thresholds and sweep-unused config floors. The pre-task checklist should now include: "update scripts/release-skip-manifest.mjs derivation when package count changes shape".
## 26-09-09 16:50 — create-plan boilerplate vs two-slice plan

create-plan skill required reading default.md which 404s, then prism-wiki.md, then full Karpathy/OKF fetches plus many graft calls before a single plan file. For a “write plan 069 covering X+Y” request, a shorter required inventory (existing plans 032/050 + current command list) would have been enough; the 7-task template forced repeating Documentation/Wiki Assessment seven times.
## 26-09-12 00:07 — release version literal sweep

Task 20 (0.5.7 cut) paid the same frozen-version-literal tax VENT 26-09-01 already described, now with a sharper edge: 26 files hardcode the current version (17 package suites' `^0.5.6` peer assertions, `docs.test.ts` ×7, `packaging.test.ts` ×7, phase24/phase27/phase34 freeze tests, benchmark scenario stamp, workflow tag lists). A mechanical `grep 0.5.6` sweep MISSES the escaped-regex variants (`0\.5\.6` in `src/__tests__/release.test.ts` and `packaging.test.ts`), which are live gates — so the cut failed a full `sdk:ready` run (typecheck + test + coverage + pack + gate, ~20 min) on two assertions I could not see with the sweep I had. What would prevent it: export a `currentVersion()` from `scripts/package-truth.mjs`, make freeze tests derive from it, and add a gate that greps for any version literal outside CHANGELOG/docs/history so the next cut is a one-file edit and the gate names every remaining site.
## 26-09-12 00:07 — sdk:ready vs CI phase loop

`npm run sdk:ready` is not the chain the release actually runs, and running it as documented fails for an environmental reason the workflow works around by hand. `.github/workflows/release.yml` reimplements sdk:ready as its own `phase()` loop precisely so that `PRISM_TEST_POSTGRES_URL` is scoped to the `release:gate` phase only ("deliberately NOT ambient here") — with the env exported for the whole run, `npm test` fails in two places (the credential-gated durable legs, and the packed-install enterprise journey, which `await import("pg")`s inside a consumer where pg is an uninstalled peer and dies ERR_MODULE_NOT_FOUND instead of skipping). So the AC "sdk:ready green end-to-end" cannot be satisfied by the npm script itself; I had to reconstruct CI's phase loop, and the postgres legs needed a locally provisioned pgvector/pg16 container whose credentials the workflow hardcodes. What would prevent it: make `sdk:ready` do what CI does (a `scripts/sdk-ready.mjs` with the phase list + env scoping, consumed by both package.json and the workflow), and make the journey fixture probe pg resolvability instead of treating the env as proof the driver exists.
